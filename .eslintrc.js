// ESLint configuration
// A configuração principal está no package.json na seção "eslintConfig"
// Este arquivo existe apenas para evitar erros de build no Railway

module.exports = {
  extends: [
    'react-app',
    'react-app/jest'
  ]
}; 